apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: oidc
  namespace: core-grafana-alloy
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password
  target:
    name: oidc
    creationPolicy: Owner
  data:
    - secretKey: pluginSecret
      remoteRef:
        key: addons.alloy.secrets
        property: oidc-secret

    - secretKey: clientId
      remoteRef:
        key: pocketid.oidc.alloy
        property: client_id

    - secretKey: clientSecret
      remoteRef:
        key: pocketid.oidc.alloy
        property: client_secret
