apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: oidc
  namespace: core-traefik
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password
  target:
    name: oidc
    creationPolicy: Owner
  data:
    - secretKey: pluginSecret
      remoteRef:
        key: addons.traefik.secrets
        property: oidc-secret

    - secretKey: clientId
      remoteRef:
        key: pocketid.oidc.traefik
        property: client_id

    - secretKey: clientSecret
      remoteRef:
        key: pocketid.oidc.traefik
        property: client_secret
