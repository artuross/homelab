apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/instance: kite
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: kite
    app.kubernetes.io/version: v0.8.0
    helm.sh/chart: kite-0.8.0
  name: kite
  namespace: core-kite
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: kite
      app.kubernetes.io/name: kite
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: kite
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/name: kite
        app.kubernetes.io/version: v0.8.0
        helm.sh/chart: kite-0.8.0
    spec:
      containers:
        - command:
            - /app/kite
          env: null
          envFrom:
            - secretRef:
                name: kite-secret
          image: ghcr.io/kite-org/kite:v0.8.0
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          name: kite
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts: null
      serviceAccountName: kite
      volumes: null
