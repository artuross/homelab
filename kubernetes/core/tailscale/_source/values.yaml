operatorConfig:
  defaultTags:
    - "tag:homelab-k8s-operator"
  resources:
    requests:
      cpu: 20m
      memory: 64Mi
    limits:
      memory: 64Mi

proxyConfig:
  defaultTags: "tag:homelab-k8s-service"

